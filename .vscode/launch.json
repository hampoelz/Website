{
    "configurations": [
        {
            "type": "pwa-msedge",
            "request": "launch",
            "name": "Launch Edge",
            "url": "${workspaceFolder}/index.html?preview=1",
            "webRoot": "${workspaceFolder}"
        }
    ]
}